package engine.audio.events;

import javax.sound.sampled.LineEvent;

import engine.audio.util.Sound;

public class AudioEventListener extends AbstractListener {

	@Override
	public void update(LineEvent arg0, Sound s) {
		
	}

}
